# Static browser runtime for local photo review

The application will be a static HTTPS frontend deployed without a Python or Flask runtime. On macOS Chrome and Edge, it will use browser-granted directory handles for local JPEG analysis and complete photo-group movement; the movement journal remains beside the files so recovery does not depend on browser storage. Safari and Firefox are not formal support targets, and the experimental MLX AI opinion is removed rather than replaced by a remote service.

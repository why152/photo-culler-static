# Photo Culler Local

一个部署到 HTTPS 静态服务器的本地照片初筛工具。运行时只有 HTML、CSS 和浏览器 JavaScript：照片、路径、技术分数与筛选进度都保留在你的电脑上，网站不会上传或接收照片。

## 支持范围

- macOS 上的 Chrome 或 Edge。
- 必须从 HTTPS 静态站点打开；`file://` 和普通 HTTP 不是受支持方式。
- 首次选择、重新打开页面或浏览器撤销授权后，浏览器会要求你重新授予照片目录的读写权限。
- Safari、Firefox、移动端、RAW 解码、递归扫描、自动永久删除、PWA 与 AI 二次判断不在支持范围内。

## 部署

将 [`web/`](./web/) 目录作为任意静态服务器的站点根目录部署即可。它不需要 Python、Flask、数据库、端口转发、账户或后端 API。

发布后用 HTTPS 域名打开页面，点击“选择照片文件夹”，在系统选择器中选择一个已从相机复制到本机的单层目录。浏览器只会在你选择的目录范围内读取或写入文件。

## 使用与安全边界

1. 页面只扫描当前目录的 JPEG；同 stem 的 `.JPG/.JPEG`、`.RW2` 与 `.XMP` 是一个 **photo group**。RAW-only 文件不会显示，RW2/XMP 不会被解码。
2. 浏览器 worker 对 JPEG 做保守技术初筛。全黑、全白、低清晰度和无法读取的 JPEG 会进入人工确认范围；分数不作构图或审美淘汰。
3. `1 / 2 / X` 保存精选、保留和建议移出决定，并移动到可见队列中的相邻下一张；`Space / J` 只跳到整个照片队列中的下一张未筛项目。
4. `C` 标记一个完整 photo group 待移动。点击“移动标记项”后，全部成员会移动到同目录的 `_PhotoCull_Review_*/`。
5. 移动前和每个成员移动后，网页都会把 `move-manifest.json` 写入该 review batch。刷新、关闭网页或中断后，重新选择源目录即可发现该 batch 并整批或单组恢复。
6. 恢复从不覆盖源目录中后来出现的同名文件；出现冲突时会停止并保留现状，供你人工处理。

`move-manifest.json` 是文件恢复的权威记录；IndexedDB 只保存缩略图、技术分析和审核界面进度。清理浏览器数据可能要求你重新选择目录，但不会删除照片目录内的 review batch。

## 本地验证

需要 Node.js 22 或更高版本：

```bash
npm test
npm run check
```

`npm test` 会运行内存测试和 Chrome 端到端测试。后者以静态服务器提供 `web/`，在浏览器的隔离本地文件系统中创建 JPEG/RW2/XMP 测试副本，验证 worker 分析、完整移动、恢复、内容与修改时间保持以及无照片网络请求；它不会打开或写入你的照片目录。

发布前仍建议在 Chrome/Edge 中选择一个复制的测试目录，人工确认原生目录授权流程与系统文件选择器行为。
